function output = getTimeAsHumanReadableString(seconds)
%TIMEASSTRING Summary of this function goes here
%   Detailed explanation goes here
if seconds < 1
    time = seconds*1e3; % ms
    output = sprintf("%i ms", time);
elseif seconds < 60^2
    output = sprintf("%i s", seconds);
elseif seconds < 
end
end

